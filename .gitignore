target/

# A copied .env.example, which holds real secrets once it is filled in.
.env
# The exception: a public demo with no real secret at stake, committed so
# `docker compose up` needs no setup step.
!/examples/todo/.env
