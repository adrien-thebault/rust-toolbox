//! One harness per crate; the module tree mirrors `src/`.
#![allow(missing_docs, clippy::missing_panics_doc)]

mod claims_mapping;
mod password;
mod principal;
mod proxy_header;
mod refresh;
mod session;
