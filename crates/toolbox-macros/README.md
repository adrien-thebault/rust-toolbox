# toolbox-macros

The `#[derive(Entity)]` procedural macro.

| Module   | What it is                                                 |
| -------- | ---------------------------------------------------------- |
| `entity` | the `Entity` derive: attribute parsing and code generation |
