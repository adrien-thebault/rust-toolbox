# toolbox-pagination

One pagination and sort representation for query strings, protobuf and SQL
alike. `serde` and `thiserror` are its only dependencies.

| Module | What it is                                                    |
| ------ | -------------------------------------------------------------- |
| `page` | one pagination representation for query strings, protobuf and SQL |
| `sort` | sort fields, and their validation against an allowlist        |
